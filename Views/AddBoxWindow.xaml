<Window x:Class="WarehousePacking.Views.AddBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить ШК короба" SizeToContent="Height" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="ProductText" Grid.ColumnSpan="2" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="Количество" VerticalAlignment="Center" Margin="0,4"/>
        <TextBox x:Name="QtyBox" Grid.Row="1" Grid.Column="1" Margin="8,4,0,4" PreviewKeyDown="OnQtyKeyDown"/>
        <TextBlock Grid.Row="2" Text="ШК короба" VerticalAlignment="Center" Margin="0,4"/>
        <TextBox x:Name="BarcodeBox" Grid.Row="2" Grid.Column="1" Margin="8,4,0,4"/>
        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" TextWrapping="Wrap" Foreground="#555" Margin="0,6,0,10"
                   Text="Количество — сколько штук товара в коробе. Затем пропикайте штрихкод короба."/>
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" IsCancel="True"/>
            <Button Content="Сохранить" IsDefault="True" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
